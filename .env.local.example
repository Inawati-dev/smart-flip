VITE_SUPABASE_URL=https://ldqouujkjfrgtgjnsmqm.supabase.co
VITE_SUPABASE_ANON_KEY=paste_the_publishable_anon_key_here

# Client-side UX deterrent ONLY for the "Dosen" role toggle on the public
# registration form (src/pages/Register.tsx). This is NOT real access
# control — it is checked in the browser and can be bypassed trivially via
# devtools or by calling the Supabase API directly. Real enforcement of the
# "dosen" role must happen server-side (Supabase RLS/trigger). Do not commit
# a real value here — set the actual code in your local .env.local only.
VITE_DOSEN_INVITE_CODE=paste_a_local_dev_only_placeholder_here
